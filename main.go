package main

import (
	"groupie-tracker/gui"
)

// main.go - le point d'entree de l'application Groupie Tracker
// on lance juste l'interface graphique, c'est elle qui gere tout le reste

func main() {
	// c'est parti mon kiki 🎵
	gui.LancerApp()
}
